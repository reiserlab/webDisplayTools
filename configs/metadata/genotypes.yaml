# Controlled vocabulary — fly genotypes for the Arena Studio Run-details panel.
# This is a CONSTRAINED pick-list (no free text) so recorded data stays typo-free
# and mergeable — pre-register a genotype via a PR (Studio's "Save as Pull
# Request" can append here for you) for it to appear in the dropdown.
format_version: "1.0"
genotypes:
  - "w;UAS-CsChrimson"
  - "w;+;GMR-Gal4"
  - "Canton-S"
  - "w1118"
