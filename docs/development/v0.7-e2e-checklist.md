# v0.7 release — E2E check-off list

*Branch `claude/competent-montalcini-076abe` (Arena Studio v0.7 · Pattern Designer v0.10 ·
index redesign · protocol-yaml skill). Work through this before blessing + pushing to main.*

**Setup:** serve locally (`python3 .claude/nocache-server.py 8091` from the repo root),
Chrome/Edge, **hard-refresh every tab** (`Cmd+Shift+R`) — several ES modules changed.
Items marked 🔌 need the arena connected; 🐙 need the GitHub sign-in.

---

## Index page

- [x] Two big cards lead (Pattern Designer, Arena Studio) with icons; both links work
- [x] Specialty group: Arena Layout · 3D View · G6 Panel Patterns · Icon Generator · Flash Programmer · Serial Monitor — all open
- [x] Legacy group is dimmed with supersede notes; all three still open

## Studio — top bar & chrome

- [x] Footer reads `Arena Studio v0.7 | 2026-07-05 …` and **GitHub** links to the repo
- [x] No device line / no ⚡ Plugins chip; hover the **status dot** → arena (+ firmware when connected 🔌)
- [x] `N cond · M blk` shows on the **Run view sequence card** (after opening a protocol), not the top bar
- [x] **Pattern Designer ↗** (always visible): first click opens the Designer set to this arena + repo (🐙 sign-in carries over); **clicking again FOCUSES the existing Designer tab** — no duplicate tabs, unsaved Designer work kept
- [x] Designer header → **Arena Studio ↗**: focuses the existing Studio tab **without reloading it** (arena connection + any run survive); with no Studio tab open, it opens one
- [x] **Other tools** on both pages opens the index in ONE reused tab (click it twice from each page — still a single index tab); no ⚙ gear menu anywhere
- [x] File ▾ shows: New protocol · Open local file… · Open from Library… · Open from Repo… · Open demo… · Save · Save copy (download)… · Promote… · Copy conditions… · Reset protocol…
- [x] 🐙 Repo field pre-filled `reiserlab/cshl-2026-course` on a fresh browser profile; kiosk 🔒 works; **checked "Commit directly" stays green while locked**

## Help mode (?)

- [ ] ? toggles on (button fills); hovering controls shows plain-language tips in every view
- [ ] First-steps cards appear in Run / Edit / Console; ✕ dismisses; re-toggling ? brings them back
- [ ] Edit card links: **Full workflow guide ↗** (GitHub doc) and **Quick Start ↗** both open
- [ ] Hovering the three Edit zones (anywhere inside) gives the zone explanation

## Run view

- [ ] File ▾ open (any source) lands in the **Editor**, not Run
- [ ] A shared `?p=g6_2x10_smoke` URL (fresh tab) still lands in **Run** with the protocol loaded
- [ ] Run gate lists what's missing (connect / save / experimenter / genotype / bridge / patterns) and updates live
- [ ] 🔌 Missing-pattern preflight: a protocol referencing a pattern not on the SD **blocks** ▶ Run experiment with the pattern named
- [ ] 🔌 ▶ Test experiment runs un-recorded; ▶ Run experiment records; **■ STOP** interrupts mid-sequence
- [ ] 🔌🐙 Run log auto-commits to `runlogs/<bench-id>/` at run end
- [ ] FicTrac strip present; connect/disconnect mirrors the Console

## Edit view

- [ ] Toolbar is exactly: Designer | YAML · ● edited · ↶ ↷ · Settings ▾
- [ ] **Ctrl+S / Cmd+S saves** (and doesn't open the browser save dialog); File ▾ → Save works the same
- [ ] 🐙 File ▾ → **Save as…** prompts for a new filename; saving to a name that already exists in the repo **stops and asks** (Replace / choose another / Cancel) — never a silent overwrite; a plain Save back to the file's own name does NOT prompt
- [ ] File ▾ → Open demo… shows the 13-demo picker; picking one loads it
- [ ] File ▾ → Save copy (download)… downloads `*_roundtrip.yaml`; → Reset protocol… resets (Undo restores); → Copy conditions… starts D4 import (Cancel exits cleanly, protocol locked meanwhile)
- [ ] **File ▾ → New**: pre-filled with this bench's rig + today's date (check Settings; `plugins: []` is correct for CSHL rigs)
- [ ] Settings (signed out): Experiment / Rig / Pattern set on top; **Advanced… auto-open** with rig path/Browse, pattern_library, date, plugin editor
- [ ] 🐙 Settings (signed in + repo): **Advanced… starts closed**; Rig line reads "uses bench rig: …"; colocate hint under Pattern set
- [ ] Experimenter field suggests roster names; date shows "— auto-set on first save" on a new protocol and stamps on Save
- [ ] Build pattern set… (in Settings) opens the builder modal
- [ ] 🐙 Builder: **Course repo** source lists `patterns/` + this protocol's `_patterns/`; **Add referenced** seeds the set from the protocol's names and reports unresolved ones; **Commit set → repo** lands the files in `<name>_patterns/` (identical skipped, different-name refused) and the W badges flip
- [ ] W/C chips: repo-opened protocol with colocated patterns shows **W**; a typo name shows **C**
- [ ] Undo/redo across edits; YAML tab edit → Apply → Undo round-trips

## Console

- [ ] Left rail: 7 tools; default open = Patterns · Arena Trial · Step frames · Test display · FicTrac (I/O + Panel firmware closed); fits the laptop screen
- [ ] 🔌 **Patterns listing is the picker**: click a row → green highlight + ▸, big preview, 0x88 info; **works on the live SD listing** (this was the offline-only bug); auto-selects row 1 after Refresh
- [ ] 🔌 Add ▾ → From course repo… → **📚 Shared pattern library** listed first; single pattern AND whole-folder upload write to the card
- [ ] 🔌 Add ▾ local file / folder / From library… still upload
- [ ] 🔌 Arena Trial: mode picker dims fields (2→rate · 4→gain+start · 3→start); ▶ Start plays the picked pattern; auto-stop(s) stops it; ■ Stop works
- [ ] 🔌 Step frames: Load pattern → ◀ ▶ wrap at the ends; Go jumps
- [ ] 🔌 Test display: Full-field / Panel map / Orientation / Checker stream; **Panel map = 1–10 bottom row wrapping CW** (the reference); Paste… and Load .bin/.pat… work
- [ ] **Bench log**: fills below the panels, scrolls internally under heavy traffic, **never covers/squeezes the panels**; drag divider resizes + snaps; ▾ hide/show works
- [ ] Controller ▾ / Debug ▾ open, stay open for Set/Get clicks, close on click-away; raw-hex send lives in Debug ▾
- [ ] 🔌 All on / All off / ■ STOP always live
- [ ] 🔌 Panel firmware + ISP batch untouched-but-working (spot-check Get info 0xE3)
- [ ] 🔌 FicTrac: bridge connect, closed-loop Activate with a Mode-3 loaded pattern

## Pattern Designer

- [ ] Opens clean — **no orange banner**; footer `Pattern Designer v0.10 | … · GitHub` at the bottom
- [ ] LOAD ▾ trio: Open local file… / Open from Library… (built-ins for the current arena) / 🐙 Open from Repo… (shared library + protocol sets)
- [ ] Generate → 2D grid + 3D view; **Panel numbers row-major in BOTH** (1–10 bottom row, 11–20 top)
- [ ] 🐙 ⇪ SAVE TO REPO…: shared library destination commits to `patterns/`; protocol destination previews `protocols/<bench>/<name>_patterns/…`; overwrite of an existing name asks first
- [ ] URL: arena change writes `?arena=…`; opening with `?arena=G6_2x8of10` applies + locks the selector
- [ ] Compare A/B, clipboard tabs, capture buttons, frame nav — unchanged and working
- [ ] 🔌 **Direction spot-check**: a fresh CW pattern → repo → SD → arena plays CW (already verified once; one more with a course pattern)

## Protocol tooling

- [ ] `~/.pixi/bin/pixi run node --import ./tests/vendor-yaml.register.mjs .claude/skills/protocol-yaml/bin/validate-protocol.mjs tests/fixtures/v3_g6_2x10_smoke.yaml` → clean, exit 0
- [ ] `pixi run test` → all suites green
- [ ] Workflow guide reads right: `docs/protocol-pattern-workflow.md` (library-first Flow A, colocation Flow B)

## After pushing to main

- [ ] Watch the Pages deploy run **for the pushed SHA** (`gh run list --json headSha` — don't trust `--limit 1`)
- [ ] Live site: hard-refresh; footer timestamps match; ES modules load (Edit view renders — the catastrophic-cache check)
- [ ] Course-repo links (`?repo=…&p=…`) open from another machine

---

**Blessed by:** ____________  **Date:** ____________
